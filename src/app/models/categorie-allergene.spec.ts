import { CategorieAllergene } from './categorie-allergene';

describe('CategorieAllergene', () => {
  it('should create an instance', () => {
    expect(new CategorieAllergene()).toBeTruthy();
  });
});
